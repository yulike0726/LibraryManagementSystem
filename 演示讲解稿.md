# 图书管理系统 — 演示视频讲解稿

> 预计时长：6-8 分钟

---

## 一、开场（30秒）

**画面：** 项目文件夹 + README.md

> 大家好，我们小组做的是一个图书管理系统。B/S 架构，Java 后端，前端 HTML5 + CSS3 + JS，数据用 JSON 文件存储，没有依赖任何第三方框架。

**语速提示：** 不快不慢，稳。

---

## 二、启动系统（30秒）

**画面：** 双击 `启动.bat` → 自动编译 → 输出访问地址

> 项目提供了一键启动脚本，双击就能自动编译、找空闲端口、启动服务器，浏览器会自动打开。

**画面：** 浏览器打开登录页 `http://localhost:8888`

> 启动后进入登录页面。管理员和读者用的是同一个登录框，输入账号后系统自动识别身份，不需要切换标签。

---

## 三、管理员功能演示（3分钟）

### 3.1 管理员登录（15秒）

**操作：** 输入 `admin` / `admin123`，点击登录

> 先用管理员账号登录。管理员拥有全部管理权限。

### 3.2 首页（20秒）

**画面：** 展示首页统计卡片 + 图书检索 + 热门排行

> 登录后看到首页仪表盘，顶部四个统计卡片显示馆藏图书数、注册读者数、借出中和超期未还数量。下面是图书检索和热门排行榜。

### 3.3 图书检索与排序（40秒）

**操作：** 在搜索框输入"Java"→ 下拉选分类"算法"→ 排序选"按书名"→ 算法选"归并排序"→ 点搜索

> 图书检索支持多条件组合——关键词模糊搜索匹配书名、作者、ISBN 和分类，同时可以按分类筛选。排序有四个字段可选，三种排序算法可以切换——快速排序、归并排序和冒泡排序。这里我们选归并排序按书名排，结果正确。

### 3.4 图书管理（40秒）

**操作：** 点击"图书管理"→ 展示列表 → 点"添加图书"→ 填写信息 → 提交 → 列表中确认出现

> 点击导航栏的图书管理，进入管理页面。这里有完整的增删改查。我们来添加一本新书——输入 ISBN、书名、作者等信息，提交后列表中能立刻看到。

### 3.5 读者管理（50秒）

**操作：** 点击"读者管理"→ 搜索某读者 → 点编辑 → 看到编辑弹窗 → 强调重置密码按钮 → 取消

> 读者管理页面可以查看所有读者、搜索、编辑和删除。注意编辑弹窗里有一个"重置密码"按钮，管理员可以一键把任何读者的密码重置为默认密码 123456。系统对读者信息更新采用了部分字段合并策略，只覆盖传入的非空字段，不会误清空其他数据。

### 3.6 借阅管理（30秒）

**操作：** 点击"借阅管理"→ 输入 ISBN 和读者 ID → 借书 → 展示结果

> 借阅管理页面可以借书、还书、续借，还支持按读者 ID 和状态筛选借阅记录。我们来演示一笔借书——输入图书 ISBN 和读者 ID，系统自动校验库存和借阅上限。

---

## 四、读者功能演示（1分半）

### 4.1 读者注册（30秒）

**操作：** 退出登录 → 回到登录页 → 点"立即注册"→ 填写学号、姓名、密码 → 注册成功

> 现在演示读者端。读者如果没有账号，可以在登录页点"立即注册"自助注册。填写学号、姓名、密码等基本信息，选择学生或教师身份。学生最多借5本、借期30天，教师最多借10本、借期60天。注册成功后返回登录。

### 4.2 读者登录与首页（30秒）

**操作：** 用刚注册的账号登录 → 展示读者首页

> 用新注册的账号登录。读者看到的首页和管理员不同——显示欢迎信息、当前借阅数、借阅上限和超期数量，没有统计数据。导航栏也只有首页和借阅管理两个入口，看不到图书管理和读者管理。

### 4.3 读者借书（30秒）

**操作：** 去借阅管理 → 借一本书 → 看到借阅记录

> 读者在借阅管理页面输入 ISBN 发起借书，系统自动校验是否有库存、是否达到借阅上限、是否已借过同一本书。借书成功后记录里可以看到状态是"借出中"，还有还书和续借按钮。

---

## 五、边界情况展示（1分钟）

### 5.1 库存不足（15秒）

**操作：** 尝试借一本库存为 0 的书

> 当图书库存为 0 时，系统提示"该图书已全部借出"。

### 5.2 借阅上限（15秒）

**操作：** 学生借满 5 本后再借

> 当学生读者借满 5 本后再次借书，系统提示"借阅数量已达上限"。

### 5.3 重复借阅（10秒）

**操作：** 借已借过的同一本书

> 读者不能重复借同一本未归还的书，系统会拦截。

### 5.4 续借限制（10秒）

**操作：** 续借已续过一次的书

> 每本书只能续借一次，再次续借会提示"不能再次续借"。

### 5.5 登录校验（10秒）

**操作：** 用错误密码登录

> 登录时输入错误密码或不存在账号，系统给出明确提示。

---

## 六、技术亮点总结（40秒）

**画面：** 可切到项目源码，快速翻看关键代码文件，或放架构图

> 最后总结一下技术亮点：
>
> 第一，**自实现 JSON 解析器**——用递归下降算法手写了一个完整的 JSON 解析器，不依赖 Gson 或 Jackson，序列化和反序列化都通过 Java 反射实现。
>
> 第二，**三种排序算法**——快速排序、归并排序、冒泡排序都可以在前端自由切换，均用 Java 泛型 + Comparator 实现。
>
> 第三，**借阅状态机**——用枚举 + 状态转换规则管理借阅状态流转，比 if-else 嵌套更清晰。
>
> 第四，**部分字段更新**——读者信息编辑时只更新传入的非空字段，保护 currentBorrows 等系统维护的数据不被意外篡改。
>
> 第五，**统一登录入口**——管理员和读者同框登录，后台自动识别角色，简化用户操作。
>
> 系统采用三层架构，Controller-Service-DAO 层次分明，各模块通过 RESTful API 松耦合协作。

---

## 七、结尾（15秒）

**画面：** 回到系统首页

> 以上就是我们小组的图书管理系统，感谢观看。

---

## 录制小贴士

| 要点 | 说明 |
|------|------|
| 讲话速度 | 偏慢、清楚，不要赶 |
| 鼠标操作 | 每一步操作前停顿半秒，让观众看到 |
| 画面 | 提前把浏览器缩放调好，字别太小 |
| 错误处理 | 如果操作出错，冷静重试，反而显得真实 |
| 时长 | 控制在 6-8 分钟，超时可以适当精简边界测试部分 |
